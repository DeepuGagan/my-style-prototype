import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-style-instyle-poc',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
